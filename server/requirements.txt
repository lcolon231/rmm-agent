# Locked and tested with CPython 3.12. Do not install this lock set with 3.14;
# several pinned native dependencies do not publish CPython 3.14 wheels.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
cryptography==44.0.0
bcrypt==4.2.1
python-multipart==0.0.20
httpx==0.28.1
websockets==14.1
# IANA timezone database for zoneinfo. Required so recurring maintenance windows
# (issue #52) and scheduled-task cron timezones resolve correctly on Windows and
# slim Linux images that lack a system tz database.
tzdata==2024.2
# Only needed when anchor_publish_backend=s3 (issue #76); lazy-imported, so
# deployments not publishing to S3 do not load it.
boto3==1.35.99
